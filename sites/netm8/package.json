{
	"name": "@factory/netm8-site",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"dry-run": "wrangler deploy --dry-run",
		"types": "wrangler types --include-runtime=false",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"hono": "^4.11.9"
	}
}
